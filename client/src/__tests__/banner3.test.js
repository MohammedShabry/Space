import { render, screen } from '@testing-library/react';
import Banner3 from '../components/Banner3';

test('renders Banner3 without crashing', () => {
    render(<Banner3 />);
  });

  
  test('renders Banner without data without crashing', () => {
    render(<Banner3 />);
  });
  

  